---
const { platform, platformUrl = `www.${platform}.com`, username = "britter" } = Astro.props;
---
<a href={`https://${platformUrl}/${username}`}>{platform}</a>

<style>
	a {
		padding: 0.5rem 1rem;
		color: white;
		background-color: #4c1d95;
		text-decoration: none;
	}
</style>
